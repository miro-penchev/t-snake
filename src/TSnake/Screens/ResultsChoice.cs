namespace TSnake.Screens;

/// <summary>
/// The footer action the player picks on the results screen (plan 07 §2.5). The order matches the
/// on-screen footer so the selection index maps straight onto it.
/// </summary>
public enum ResultsChoice
{
    PlayAgain,
    MainMenu,
    Quit,
}
